{
  "name": "@accord/execution",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint src --ext .ts --ignore-pattern 'src/**/*.test.ts'",
    "test": "vitest run --config vitest.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@accord/core": "workspace:*",
    "@visaacceptance/agent-toolkit": "^0.0.96",
    "zod": "^4.1.12"
  },
  "devDependencies": {
    "@types/node": "^24.10.1"
  }
}
